* Configurable twemoji endpoints
* Embedded backup twemoji
* Other emoji fonts, configurable. button in config gui that lets the user cycle between available fonts (keep a sample of each in resources). Only download and apply if config is applied.
* fix config buttons
* webp and jpg support
